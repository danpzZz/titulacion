import { Routes } from '@angular/router';

// Archivo conservado solo para compatibilidad con la estructura original.
// Las rutas reales están en secretary.routes.ts.
export const secretaryRoutesCompatibility: Routes = [];
