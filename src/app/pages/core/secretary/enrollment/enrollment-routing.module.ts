import { Routes } from '@angular/router';

// Archivo conservado solo para compatibilidad con la estructura original.
// Las rutas reales del proyecto Angular 21 standalone están en:
// src/app/pages/core/secretary/secretary.routes.ts
export const enrollmentRoutes: Routes = [];
